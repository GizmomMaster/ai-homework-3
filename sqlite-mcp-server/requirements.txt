# This is a Node.js/TypeScript project, not Python — there is no pip/PyPI
# dependency tree. This file is a plain-text mirror of the requirements
# declared in package.json / package-lock.json (the actual source of truth).
# To install, use npm — see README.md → Install.

Runtime:
  node >= 18

Package manager:
  npm

Dependencies (npm install):
  @modelcontextprotocol/sdk ^1.12.0
  better-sqlite3            ^11.9.1
  zod                       ^3.24.2

Dev dependencies (npm install, build/dev only):
  @types/better-sqlite3 ^7.6.12
  @types/node           ^22.13.5
  tsx                    ^4.19.3
  typescript             ^5.7.3

Install command:
  npm install

Build command:
  npm run build
